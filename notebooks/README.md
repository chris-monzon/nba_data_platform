# notebooks/

Exploratory analysis only — **not part of the pipeline**. Use for EDA, sanity checks, and
prototyping. Anything that becomes production logic moves into `ingestion/`, `transform/`, or `app/`.
